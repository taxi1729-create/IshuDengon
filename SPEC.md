# 異種伝言ゲーム (ishu_dengon) 仕様書

## 1. ゲーム概要

| 項目 | 内容 |
|------|------|
| タイトル | 異種伝言ゲーム（ishu_dengon） |
| プレイ人数 | 2〜5人 |
| プラットフォーム | スマートフォン（縦画面） |
| ジャンル | パーティーゲーム / 伝言ゲーム |

### コンセプト
各プレイヤーが異なる「アクション（表現手段）」を使ってお題を次の人に伝えていく伝言ゲーム。最後の人が口頭で正解を発表し、最初の人が正誤を判定する。

---

## 2. 画面フロー

```
タイトル画面
  └─→ 設定（人数・難易度・音量）
        └─→ ゲームプレイ画面
              ├─→ [1人目] お題確認 → アクション選択 → 実行
              ├─→ [2〜N-1人目] 前の人の内容を受け取る → アクション選択 → 実行
              ├─→ [最後の人] 前の人の内容を受け取る → 口頭で答え発表
              └─→ リザルト画面（正解 / 不正解）
```

---

## 3. 画面別仕様

### 3.1 タイトル画面

#### 機能
- ゲームタイトル表示
- **人数設定**：2〜5人をセレクト（デフォルト3人）
- **難易度設定**：「やさしい」「ふつう」「むずかしい」の3段階
- **音量設定**：スライダー（シークバー）で0〜100%制御
- 「ゲームスタート」ボタン

#### UI要素
```
[タイトルロゴ]
[人数: 2 3 [4] 5]     ← トグルボタン
[難易度: やさしい / ふつう / むずかしい]  ← セグメントコントロール
[🔊 音量 ━━●━━ ]     ← スライダー
[ゲームスタート ▶]
```

---

### 3.2 ゲームプレイ画面（1人目）

#### 処理フロー
1. 「〇〇さんの番です。他の人は見ないでください」と表示
2. 準備ができたら「タップして確認」
3. お題が表示される
4. 以下のアクションから1つを選択する

#### アクション一覧（1人目用：全4種）

| # | アクション名 | 内容 |
|---|-------------|------|
| 1 | **AI変換** | お題をAIが「尖った視点」で言い換える（例：リンゴ→人類を堕落させた禁断の果実）。AIが生成した文章を次の人に見せる |
| 2 | **絵を描く** | キャンバスに手描きで絵を描く。次の人にその絵を見せる |
| 3 | **ジェスチャー** | 身振り手振りで伝える。「ジェスチャー中」の表示のみ。次の人は見て推測する |
| 4 | **抽象図形** | 画面上に図形（正三角形・菱形・円・正方形など）をドラッグ配置し、色を変更して表現する |

> **注意**：最後から1人前のプレイヤーのみ「口頭（縛り付き）」アクションが選択可能

---

### 3.3 ゲームプレイ画面（2人目〜最後から1人前）

#### 処理フロー
1. 「〇〇さんの番です。他の人は見ないでください」
2. 前の人が作ったコンテンツ（絵 / AI文章 / 図形配置）を表示
3. アクションを選択して次の人に伝える

#### アクション一覧（中間プレイヤー用：4〜5種）

最後から1人前のプレイヤーのみ、以下が追加される：

| # | アクション名 | 内容 |
|---|-------------|------|
| 5 | **口頭（縛り付き）** | 言葉で伝えるが、以下の縛りがランダムでつく |

#### 口頭縛りルール（ランダム選択）
- 文字数制限（例：10文字以内で説明）
- 濁音禁止（が行・ざ行・だ行・ば行・ぱ行禁止）
- 語尾縛り（例：全て「〜にゃ」で終わらせる）
- 疑問文のみ（全て質問形式で説明）
- オノマトペのみ（擬音語・擬態語のみで説明）

---

### 3.4 ゲームプレイ画面（最後の人）

#### 処理フロー
1. 「〇〇さんの番です。他の人は見ないでください」
2. 前の人のコンテンツを表示
3. 画面に答えを入力するか口頭で発表し、最初の人が正解ボタン / 不正解ボタンを押す

#### UI要素
- 前の人のコンテンツ表示
- 「正解！」ボタン（緑）
- 「不正解…」ボタン（赤）

---

### 3.5 リザルト画面

#### 正解時の表示
- 「🎉 正解！」アニメーション
- お題の振り返り
- 各プレイヤーのアクションと内容を順番に表示
  - AI変換 → テキスト表示
  - 絵を描く → 描いた絵（画像）を表示
  - 抽象図形 → 配置した図形を表示
  - ジェスチャー → 「ジェスチャーでした！」と表示
  - 口頭 → 「口頭（縛り: ○○）でした！」と表示

#### 不正解時の表示
- 「😢 不正解…」アニメーション
- お題の正解を発表
- 各アクション内容の振り返り

#### ボタン
- 「もう一度遊ぶ」→ タイトルへ
- 「同じ設定で続ける」→ 次のラウンドへ

---

## 4. お題リスト

### やさしい難易度（例）
- りんご、犬、自転車、傘、サッカー、ピザ、富士山、電車、太陽、本

### ふつう難易度（例）
- 二日酔い、渋滞、試験前夜、告白、引越し、失恋、初めてのデート、友情

### むずかしい難易度（例）
- 量子もつれ、哲学的ゾンビ、デジャブ、集合的無意識、バタフライ効果、時間の矛盾

> お題は `topics.js` で難易度ごとに配列として管理。追加・編集が容易な構造。

---

## 5. サウンド仕様

| イベント | サウンド |
|---------|---------|
| 次へボタン押下 | ポジティブな効果音 |
| 不正解ボタン押下 | ネガティブな効果音 |
| 正解判定 | ファンファーレ |
| アクション選択 | 軽いクリック音 |

- **バイブレーション**：次へ・不正解ボタン押下時に `navigator.vibrate()` を使用
- **音量**：タイトル画面のスライダーで制御（Web Audio API）

---

## 6. AI変換アクション仕様

- Anthropic API（claude-sonnet-4-20250514）を使用
- プロンプト例：
  ```
  以下のお題を、尖った・哲学的・詩的・誇張した視点で1〜2文に言い換えてください。
  ユーモアや皮肉を含めてOKです。
  お題：{topic}
  ```
- ローディング表示あり
- APIキーは `config.js` で管理

---

## 7. 絵を描くアクション仕様

- HTML5 Canvas を使用
- タッチ操作対応（`touchstart`, `touchmove`, `touchend`）
- ペンの太さ選択：細・中・太
- 消しゴム機能
- 全消去ボタン
- 完成したらキャンバスを `toDataURL()` でBase64保存

---

## 8. 抽象図形アクション仕様

### 使用可能な図形
- 正三角形、菱形、円、正方形、星形、六角形

### 操作
- **追加**：図形ボタンをタップ → キャンバスに配置
- **移動**：ドラッグ
- **色変更**：図形を選択 → カラーパレットから選択
- **削除**：図形を長押し → 削除ボタン
- **サイズ変更**：ピンチ操作またはスライダー

---

## 9. ファイル構成（GitHub Pages対応）

```
ishu_dengon/
├── index.html          # エントリーポイント
├── style.css           # グローバルスタイル
├── config.js           # API設定（キーなど）
├── topics.js           # お題リスト（難易度別）
├── game.js             # ゲームロジック・状態管理
├── ui.js               # 画面遷移・UI制御
├── actions/
│   ├── ai-transform.js  # AI変換アクション
│   ├── drawing.js       # お絵描きアクション
│   ├── gesture.js       # ジェスチャーアクション
│   ├── shapes.js        # 抽象図形アクション
│   └── verbal.js        # 口頭アクション
├── sounds/
│   ├── next.mp3
│   ├── wrong.mp3
│   └── correct.mp3
└── SPEC.md             # 本仕様書
```

---

## 10. GitHub Pages デプロイ手順

1. GitHubで新リポジトリ `ishu_dengon` を作成
2. 全ファイルをルートに配置してpush
3. Settings → Pages → Source: `main` ブランチ / `/ (root)` を選択
4. `https://{username}.github.io/ishu_dengon/` でアクセス可能

---

## 11. 拡張性・保守性の方針

- お題はすべて `topics.js` に集約。難易度をキーにしたオブジェクト形式
- アクションは `actions/` フォルダ以下にモジュール分離
- 将来的にバックエンドなしで動くよう、状態はすべてJavaScriptオブジェクトで管理
- サウンドは差し替えやすいよう `sounds/` フォルダに集約

---

## 12. 技術スタック

| 技術 | 用途 |
|-----|-----|
| HTML5 / CSS3 / Vanilla JS | フロント全般 |
| HTML5 Canvas API | お絵描き・図形 |
| Web Audio API | サウンド制御 |
| Vibration API | バイブレーション |
| Anthropic API | AI変換アクション |
| GitHub Pages | ホスティング |
